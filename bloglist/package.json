{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "fullstackopen",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "test": "NODE_ENV=test jest --verbose --runInBand",
    "dev": "nodemon index.js",
    "lint": "eslint .",
    "lintfit": "eslint . --fix",
    "build": "rm -rf build && cd bloglist-frontend/ && npm run build --prod && cp -r build ../"
  },
  "author": "someone me",
  "license": "MIT",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.0.5",
    "mongoose-unique-validator": "^2.0.3",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.12"
  },
  "devDependencies": {
    "eslint": "^7.32.0",
    "jest": "^27.2.0",
    "supertest": "^6.1.6"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
